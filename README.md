# Clone-of-mailchimp
 About Mailchimp clone build on react js using Firebase as Backend and firestore database, we use Material UI , Chakra UI and SCSS in frontend, Paypal as payment gateway and we use React Redux and Context APIs to configure connections between components and dynamic Search Section. For Login and Signup we use Firebase Auth and mail services
